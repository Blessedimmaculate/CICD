name: first workflow
on: push
jobs:
  my-job:
     runs-on: ubuntu-latest
     steps:
        - name: clone repo
          uses: actions/checkout@v4
        # - name: first-step
        #   run: echo "Hello World"
        - name: Read-File
          run: cat hello-world.txt